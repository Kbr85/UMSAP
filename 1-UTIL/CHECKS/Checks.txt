-------------------------------------------------------- BEFORE RELEASING (Down)
	
	-- Update config.urlTutorial

	-- Change Mac path for pathResources so the bundle works properly

	-- Check that config.version and config.versionUpdate has the up to date 
		value

	-- Remove any NON-default path and options from Modules and Utilities

-------------------------------------------------------- BEFORE REALISING (UP)



------------------------------------------------------------ MANUAL (Down)

	-- Short cuts are growing place a list in the manual
		Ctrl+D will close the current window. Implemented in gclasses.WinMyFrame
		Others are in Menu

	-- Discuss that Histograms windows are silently modified.

	-- Discuss that when reading a uscr file incorrect or missing keywords for a
		module are silently ignored because when running the module the first
		check will take care of detecting missing configuration options in the
		module.

		Checkbox and Combobox with wrong keywords values are set to default 
		values

	-- Discuss that when creating a uscr file if the UMSAP file was modified
		and keywords were remove, included, altered the resulting wrong
		keyword are not passed to the uscr file. This may cause that
		TypeRes does not work properly due to missing key words in the uscr 
		file

	-- TypeRes when reading tcResults if something goes wrong the windows is 
		reset to default empty values

------------------------------------------------------------ MANUAL (Up)



------------------------------------------------------------ THINGS TO DO (Down)

  ---------------------------------------------------- GENERAL

	-- You need a document describing the structure of the data frame used
		in the application. ALL OF THEM

	-- Menu should be redone with a scheme like the one your are using for 
		ProtProfR.Tools.Filter_Results_by
		In essence you will have Menu Instances that will be used to 
		create PopUpMenus and MenuBars
	
		This is old:
		Tool menus should be slightly different in popup and menubar.
		E.g. The tool menu in TarProt has a header that makes not much sense
		in the menubar

		Check all menus. For example, the Tools menu in CorrARes makes no 
		sense but you already tried to have a submenu for Export Data to 
		and it crashes. Give it another try.

	-- Errors when checking tcResults do not clearly say what it is wrong.

	-- Get rid of the SettingWithCopyWarning coming from Pandas in all UMSAP

	-- The fixed part of the text for the sections in the analysis should be
		in a dict in config.

	-- Change the itertupple to apply with raw=True like in protprof. It is 
		faster and easier to maintain. It also allows to change the output 
		data frame without breaking the module, just add the new values to 
		the new column.

	-- Add p value correction to all modules

	-- Make a splash window and move the import in UMSAP below the splash 
		window because now startup is slow because all the imports in UMSAP

	-- gclasses Helper in data_methods should be move to a base class in 
		gui_classes. 
		
		The same goes for all other module helpers

	-- Add index to the pdf file created for the sequence alignment and 
		highlight.

	-- Redesign class again because you could use some abstract parent class
		to hold methods used in different modules.

	-- CheckGui classes should accept both a wx.TextCtrl or a value because
		otherwise you cannot use them to check arbitrary values. The idea is to 
		keep methods in check and check_multiple just checking and returning
		[True/False, Original_value/Modified_value] and let GuiCheck to handle 
		error notification.

	-- Create a way to generate plot of data to show normalization 
		improvement 
		
		BASICALLY YOU NEED A NORMALIZATION UTILITY.

	-- Report unpredicted error. Scrolled message and custom dialog.

	-- Export data in human readable format in all graph res window

	-- Probably you will need to allow Mod/Util windows with a ListBox to 
		resize to be able to read long items in the Listbox

	-- The Sequence Highlight limprot should be move to dclasses.MyModule to 
		use it in tarprot too.

	-- Currently only a minimum screen height of 900 px is supported

	OLD FILE BELOW

	-- Error message when searching UNIPROT/PDB.

	-- SET/UPDATE THE LINK TO TUTORIALS

  ---------------------------------------------------- GENERAL

  
  
  ---------------------------------------------------- MODULES

	------------------------------------- LIMPROT (Down) EMPTY

		--

	------------------------------------- LIMPROT (UP)



	------------------------------------- PROTPROF (Down) EMPTY

		--

	------------------------------------- PROTPROF (Up)



	------------------------------------- TARPROT (Down)

		-- Add a feature to analyse the length of the peptides detected and show
			the results as an histogram based on peptide length from 0 to Rec 
			Prot length

		-- Add the sequence highlight pdf creation. Also, the sequence alignment
			can be moved to this PDF. 

		-- Offer several methods: ANCOVA, ANOVA, t-test with correction and TOST

		-- Sometimes Uniprot and PDB takes time make a warning

		-- Generating the pdb takes a while. Improve

		OLD FILE BELOW

		-- Add Sequence coverage

	------------------------------------- TARPROT (Up)

  ---------------------------------------------------- MODULES



  ---------------------------------------------------- UTILITIES

    ------------------------------------- GENERAL

		----- TYPERES (DOWN)

			-- TarProt could name control and experiments like ProtProf

		----- TYPERES (UP)



		----- CORRELATION ANALYSIS UTIL (Down)

			OLD FILE BELOW

			-- Add a way to reorder columns in Correlation analysis utility 
				window

		----- CORRELATION ANALYSIS UTIL (Up)



		----- CREATE INPUT FILE UTIL (Down) EMPTY

			--

		----- CREATE INPUT FILE UTIL (Up)



		----- INPUT SCRIPT UTIL (Down) EMPTY

			--

		----- INPUT SCRIPT UTIL (Up)



		----- SHORT DATA FILE UTIL (Down)

			-- It must show the list of columns to make it easier

		----- SHORT DATA FILE UTIL (Up)

	------------------------------------- GENERAL



	------------------------------------- TARPROT

		----- AA DIST UTIL (Down)

			-- Full support for adding extra amino acids with the syntax pY

			-- Graph absolute values and % Both

			OLD FILE BELOW

			-- Compare chi^2 AA dist from 2 conditions in the same .aadist to 
				see if the distributions are different between two given 
				conditions

			-- Retake the comparison between aadist files.	

		----- AA DIST UTIL (Up)



		----- CUTPROP UTIL (DOWN)

			-- Improve TarProt2CutProp, SetAxis, Draw

		----- CUTPROP UTIL (UP)
	
	
	
		----- CLEAVAGES TO PDB FILES UTIL (Down)

			-- It is to slow in the current implementation. Improve

		----- CLEAVAGES TO PDB FILES UTIL (Up)



		----- HISTOGRAMS  UTIL (Down)

			-- Improve TarProt2HistoFile

			OLD FILE BELOW

			-- Check that in a custom list the last value should be included

		----- HISTOGRAMS UTIL (UP)



		----- SEQUENCE UTIL (Down)

			-- Improve SetseqOverLapRegion

		----- SEQUENCE UTIL (Up)



		----- UPDATE RESULTS UTIL (Down)

			-- TarProt (Update utility) needs a progress dialog because of the 
				pdb slow processing

		----- UPDATE RESULTS UTIL (UP)

	------------------------------------- TARPROT



	------------------------------------- LIMPROT EMPTY

		----- SEQUENCE HIGHLIGHT (Down) EMPTY

			-- 

		----- SEQUENCE HIGHLIGHT (Up)

	------------------------------------- LIMPROT

  ---------------------------------------------------- UTILITIES



  ---------------------------------------------------- RESULTS WINDOWS

    ---------------------------------------------------- MODULES

		----- TARPROTRES RESWIN (Down)

			OLD FILE BELOW

			-- Behavior of the TarProtRes that is not optimal when creating the 
				window and comming from maximize state (paint and resize event 
				necesary in OS and 10????)

			-- Save fragments as svg, pdf and tiff

		----- TARPROTRES RESWIN (Up)



		----- LIMPROTRES RESWIN (Down) EMPTY

			--

		----- LIMPROTRES RESWIN (Up)



		----- PROTPROFRES RESWIN (Down)

			-- Filter Results options
	
			-- Same scale in both plot. At least as an option in the menus

		----- PROTPROFRES RESWIN (Up)

 	---------------------------------------------------- MODULES



	---------------------------------------------------- UTILITIES

		----- CORRELATION ANALYSIS UTIL (Down)

			-- Show and hide colorbar

			-- Square window size changes

		----- CORRELATION ANALYSIS UTIL (Up)

	---------------------------------------------------- UTILITIES

  ---------------------------------------------------- RESULTS WINDOWS

------------------------------------------------------------ THINGS TO DO (Up)



------------------------------------------------------------ CHANGE LOG (Down)

	-- Two new modules

	-- Easier way to type the column numbers containing the control and 
		experiment result

	-- Included threads for better GUI experience.

	-- Change : for a ; in PDBID TarProt module

	-- Improved display of columns label in CorrARes

	-- Pop up menu now works inside graphics

	-- Check for Updates 

	-- Preference panels

	-- Cutprop total column change to FP list

	-- Data_Step added for manual control of step by step results

	-- Format of output file change to json or csv to allow easier reading by
		users needing to manipulate the output files.
	
	-- List of filtered peptide in the .filtpept file simplified.

------------------------------------------------------------ CHANGE LOG (Up)







